---
title: "Derived Comprehensive Rules metadata"
status: "current"
authoritative_source: "simctl rules sync and tracked rules indexes"
verified: "2026-08-18"
audience: "rules and compiler contributors"
maintenance: "hand-maintained"
---

# Derived Comprehensive Rules metadata

These files are generated by `simctl rules sync`.

The official downloaded TXT is stored under ignored `local/rules/`. Tracked
indexes contain IDs, short headings, hashes, source line spans, dependencies,
coverage fields, and reviewed-summary placeholders—not the rules prose.
Rule IDs preserve the complete lowercase suffix, including multi-letter
subrules such as `704.5aa`; a new suffix must never be folded into the preceding
rule's hash or source span.

`conformance-cases.json` contains one stable, source-pinned work item per rule.
Its default inventory-only status proves neither implementation nor rules
correctness. See the [rules assurance model](../docs/rules/assurance-model.md) and
`coverage/rules-conformance.json`.

When the official snapshot changes, `coverage/rules-delta.json` owns the exact
added, removed, changed, renumbered, and invalidated IDs. Retarget review and
mechanic-contract headers only after comparing rule hashes. Preserve reviews
for text-identical renumberings, and remove or explicitly re-review changed
cases so source drift cannot inherit trust.

`platform/rules-subsystems.json` is the hand-maintained, prose-free subsystem
and dependency catalog. `scripts/update_rules_scheduler.py` combines it with
the rule index, conformance cases, and capability registry to generate
`coverage/rules-dependency-queue.json` and
`docs/RULES_DEPENDENCY_QUEUE.md`. Unclassified nonpassing rules stay queued
until review proves that they are structural or definition-only.

Run `simctl rules verify --root .` after regeneration. A successful structural
verification is not a completeness claim; see `coverage/rules-coverage.json`
and the [current rules claim boundary](../docs/RULES_COMPLETENESS_STATUS.md).
